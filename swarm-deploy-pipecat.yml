version: '3.9'

networks:
  unmute-net:
    external: true

services:
  traefik:
    image: traefik:v3.3.1
    command:
      - "--configFile=/etc/traefik/traefik-static.yml"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /root/traefik-static.yml:/etc/traefik/traefik-static.yml:ro
      - /root/traefik-dynamic.yml:/etc/traefik/dynamic.yml:ro
      - /root/certs:/etc/certs:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    deploy:
      placement:
        constraints:
          - node.role == manager
      update_config:
        order: stop-first
    networks:
      - unmute-net

  frontend:
    image: intellipedia/inti-frontend:v15.11.23
    environment:
      - NEXT_PUBLIC_IN_DOCKER=true
      - NEXT_PUBLIC_WS_URL=wss://inti.intellipedia.ai/v1/realtime
      - NEXT_PUBLIC_API_BASE=https://inti.intellipedia.ai/api
      - NEXT_PUBLIC_BACKEND_URL=https://inti.intellipedia.ai/api
    deploy:
      update_config:
        order: start-first
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.frontend.rule=Host(`inti.intellipedia.ai`)"
        - "traefik.http.routers.frontend.entrypoints=websecure"
        - "traefik.http.routers.frontend.tls=true"
        - "traefik.http.services.frontend.loadbalancer.server.port=3000"
        - "traefik.http.routers.frontend.priority=50"
    networks:
      - unmute-net

  backend:
    image: intellipedia/inti-groq-backend:v2.0.17
    env_file:
      - .env
    environment:
      - GROQ_ENABLED=${GROQ_ENABLED:-true}
      - GROQ_STT_MODEL=${GROQ_STT_MODEL:-whisper-large-v3-turbo}
      - GROQ_LLM_MODEL=${GROQ_LLM_MODEL:-openai/gpt-oss-20b}
      - GROQ_TTS_MODEL=${GROQ_TTS_MODEL:-playai-tts}
    deploy:
      update_config:
        order: start-first
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.backend.rule=Host(`inti.intellipedia.ai`) && PathPrefix(`/api`) && !PathPrefix(`/api/v1/`)"
        - "traefik.http.routers.backend.entrypoints=websecure"
        - "traefik.http.routers.backend.tls=true"
        - "traefik.http.routers.backend.priority=100"
        - "traefik.http.routers.backend.middlewares=backend-stripprefix"
        - "traefik.http.middlewares.backend-stripprefix.stripprefix.prefixes=/api"
        - "traefik.http.services.backend.loadbalancer.server.port=80"
    networks:
      - unmute-net

  pipecat-backend:
    image: intellipedia/inti-pipecat-backend:dev
    env_file:
      - .env
    environment:
      - INTI_ALLOW_MOCK_KEYS=${INTI_ALLOW_MOCK_KEYS:-false}
      - INTI_LOG_LEVEL=${INTI_LOG_LEVEL:-INFO}
      - INTI_SAMPLE_RATE=${INTI_SAMPLE_RATE:-48000}
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import urllib.request; urllib.request.urlopen(\"http://127.0.0.1:8000/health\")'" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    deploy:
      replicas: 1
      update_config:
        delay: 10s
        parallelism: 1
        order: start-first
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.pipecat.rule=Host(`inti.intellipedia.ai`) && PathPrefix(`/api/v1/realtime`)"
        - "traefik.http.routers.pipecat.entrypoints=websecure"
        - "traefik.http.routers.pipecat.tls=true"
        - "traefik.http.routers.pipecat.priority=300"
        - "traefik.http.routers.pipecat.middlewares=pipecat-strip,pipecat-ws"
        - "traefik.http.routers.pipecat.service=pipecat"
        - "traefik.http.routers.pipecat-direct.rule=Host(`inti.intellipedia.ai`) && PathPrefix(`/v1/realtime`)"
        - "traefik.http.routers.pipecat-direct.entrypoints=websecure"
        - "traefik.http.routers.pipecat-direct.tls=true"
        - "traefik.http.routers.pipecat-direct.priority=300"
        - "traefik.http.routers.pipecat-direct.middlewares=pipecat-ws"
        - "traefik.http.routers.pipecat-direct.service=pipecat"
        - "traefik.http.routers.pipecat-health.rule=Host(`inti.intellipedia.ai`) && PathPrefix(`/api/v1/health`)"
        - "traefik.http.routers.pipecat-health.entrypoints=websecure"
        - "traefik.http.routers.pipecat-health.tls=true"
        - "traefik.http.routers.pipecat-health.priority=320"
        - "traefik.http.routers.pipecat-health.middlewares=pipecat-strip,pipecat-health"
        - "traefik.http.routers.pipecat-health.service=pipecat"
        - "traefik.http.services.pipecat.loadbalancer.server.port=8000"
        - "traefik.http.middlewares.pipecat-strip.stripprefix.prefixes=/api"
        - "traefik.http.middlewares.pipecat-ws.replacepath.path=/ws"
        - "traefik.http.middlewares.pipecat-health.replacepath.path=/health"
    networks:
      - unmute-net
